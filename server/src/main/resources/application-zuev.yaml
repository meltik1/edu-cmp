---
spring:
  profiles: zuev
  datasource:
    url: ${SPRING_DATASOURCE_PASSWORD:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:300800}
  jpa:
    hibernate:
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:create}
server:
  port: 8080
bot:
  token: 1531954543:AAHgZt4bem3JygKOQ69JSD-oM51odOA2POg

mongo:
  uri: ${MONGO_DB_URI:mongodb://root:root@localhost:27017}

mail:
  protocol: smtp
  smtp:
    auth: true
    starttls.enable: true
    debug: true
    starttls.required: true
  from: smtp.tests.zuevc@gmail.com
  host: smtp.gmail.com
  port: 587
  username: smtp.tests.zuev@gmail.com
  password: Glass71836


